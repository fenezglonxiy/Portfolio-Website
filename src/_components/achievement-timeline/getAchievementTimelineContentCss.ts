import { css, Theme } from "@mui/material";

const getAchievementTimelineContentCss = (theme: Theme) => css`
  padding-inline: ${theme.spacing(5)};
`;

export default getAchievementTimelineContentCss;
