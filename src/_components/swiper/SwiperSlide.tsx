import {
  SwiperSlide as SwiperSwiperSlide,
  SwiperSlideProps as SwiperSwiperSlideProps,
} from "swiper/react";

export type SwiperSlideProps = SwiperSwiperSlideProps;

const SwiperSlide = SwiperSwiperSlide;

export default SwiperSlide;
