import { css, Theme } from "@mui/material";

const getOtherInsightsContentCss = (theme: Theme) => css`
  padding: ${theme.spacing(24)} ${theme.spacing(28)};
`;

export default getOtherInsightsContentCss;
