import { css, Theme } from "@mui/material";

const getHomeAchievementsShowcaseCss = (theme: Theme) => css`
  --flow-spacing-y: ${theme.spacing(12)};
`;

export default getHomeAchievementsShowcaseCss;
