import { css, Theme } from "@mui/material";

const getAboutMeSkillsContentCss = (theme: Theme) => css`
  padding: ${theme.spacing(10)} 0 ${theme.spacing(27)};
`;

export default getAboutMeSkillsContentCss;
