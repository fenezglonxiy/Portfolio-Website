import { css, Theme } from "@mui/material";

const getAboutMeSkillsCTACss = (theme: Theme) => css`
  position: relative;
  padding: ${theme.spacing(8)} 0;
  display: flex;
  justify-content: center;
`;

export default getAboutMeSkillsCTACss;
