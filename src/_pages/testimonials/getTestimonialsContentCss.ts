import { css, Theme } from "@mui/material";

const getTestimonialsContentCss = (theme: Theme) => css`
  padding: 0 0 ${theme.spacing(30)} ${theme.spacing(18)};
`;

export default getTestimonialsContentCss;
