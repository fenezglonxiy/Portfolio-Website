import { css, Theme } from "@mui/material";

const getWorkDetailsOtherWorksCss = (theme: Theme) => css`
  background-color: ${theme.palette.neutral[200]};
`;

export default getWorkDetailsOtherWorksCss;
