export type ShapeOptions = {
  sm: string;
  md: string;
  pill: string;
  circle: string;
};

const shape: ShapeOptions = {
  sm: "12px",
  md: "20px",
  pill: "999em",
  circle: "50%",
};

export default shape;
