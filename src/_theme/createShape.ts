export type ShapeOptions = {
  none: string;
  xs: string;
  sm: string;
  md: string;
  lg: string;
  pill: string;
  circle: string;
};

const shape: ShapeOptions = {
  none: "0px",
  xs: "8px",
  sm: "12px",
  md: "20px",
  lg: "32px",
  pill: "999em",
  circle: "50%",
};

export default shape;
